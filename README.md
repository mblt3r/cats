# Котики с рейтингом смешности

React-приложение с авторизацией и рейтингом смешных котиков.

## Технологии

### Фронтенд
- **React 18** - UI библиотека
- **React Router** - маршрутизация
- **Vite** - сборщик и dev сервер
- **CSS Modules** - стилизация компонентов

### Бэкенд
- **Node.js** - сервер
- **Express** - фреймворк
- **Express-session** - управление сессиями
- **bcrypt** - хеширование паролей

## Разработка

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Эта команда запустит:
- Бэкенд сервер на порту 3000
- Vite dev сервер на порту 5173
- Проксирование API запросов с фронтенда на бэкенд

### Сборка для продакшена
```bash
npm run build
```

### Запуск в продакшене
```bash
npm start
```

## Функционал

- 🔐 Авторизация и регистрация пользователей
- 🐱 Случайные котики из CATAAS API
- 🏆 Топ самых смешных котиков с возможностью голосования
- 📱 Бесконечная лента котиков с lazy loading
- 🎨 Современный UI с градиентами и анимациями

## Структура проекта

```
├── src/
│   ├── components/          # React компоненты
│   │   ├── Auth.jsx       # Страница авторизации
│   │   ├── Cats.jsx       # Главная страница с котиками
│   │   ├── RandomCat.jsx  # Компонент случайного котика
│   │   ├── TopCats.jsx    # Топ котиков
│   │   └── Feed.jsx       # Бесконечная лента
│   ├── services/           # API сервисы
│   │   └── api.js         # Функции для работы с API
│   ├── App.jsx            # Главный компонент приложения
│   ├── main.jsx           # Точка входа
│   └── index.css          # Глобальные стили
├── server/
│   ├── authRoutes.js      # Маршруты авторизации
│   ├── catsRoutes.js      # Маршруты котиков
│   ├── data/              # Хранилище данных
│   └── index.js           # Главный файл сервера
└── vite.config.js         # Конфигурация Vite
```
